enum Role { civilian, spy, mrWhite }

enum Phase { describe, vote, interrogation, results, gameOver }

enum InterrogationChallenge { secretPhrase, finalPerformance }

// Add this enum at the top of your file (or in enums.dart)
enum Language { english, arabic }
